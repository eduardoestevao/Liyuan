# 第三步 · 注入完整性与执行意图

**立档 2026-08-21 深夜。** 本文是「回归单轮」三步路线第三步（梨园架构融入酒馆）的执行契约，
取代此前一切关于「主动检索怎么触发」的口头方案。

---

## 〇、定案：问题是两个，不是一个

用户 2026-08-21 定案：

1. **注入机制不完整** —— 梨园的记忆手段里，有两条注入通道整条死了，一条实现走样。
2. **执行意图被预设覆盖** —— 模型**已经形成检索意图**，但意图在「思考 → 产出」的交界处蒸发。

**优先级：先修 ②。** ① 是供料问题，② 是执行问题；② 不修，供再多料也变不成动作。

---

## 一、被推翻的三个旧判断（别再走回去）

立此存照，避免下个窗口重犯：

| 旧判断 | 为什么错 |
|---|---|
| 「模型不查是因为不查也能写下去」 | 8/21 实测反例：怀瑾查不到它没查、星光学院查得到它也没查。检索从来不是被「写不下去」逼出来的。 |
| 「工具文案不够硬，改文案能治」 | 8/21 实验：把 `ask` ② 的锚点从死掉的「列路标之前」改成「开场第一拍」，**完全没有触发**。文案是苍白的。 |
| 「派个子代理专门去检索」 | 8/21 证伪：「从索引报编号」是纯位置偏置（反转索引，仍报 0/1/2）；更根本的是子代理**必须交差、没有『查不到』的退路**。见 [[liyuan-retrieval-subagent-falsified]]。 |

---

## 二、决定性证据：位置 > 文案

`git show HEAD:src/stage/engine.ts` 第 709–712 行，旧代码里的注释：

```
// 注入块（世界状态/索引等）压在提问之后时，模型会把提问读成历史里的旧话，
// 于是既不检索也不正面回应——8/03 实测：同一提问，挪到注入之后立刻触发 lorebook_search。
```

**同一段文字，换个位置，检索就发生。** 这条 8/03 的实测结论当时被写进了注释，后来在拆循环时连同代码一起删掉了。

### 8/21 实弹佐证（会话 `2026-08-21T16-52-38`）

思考原文（最后三句）：

> 「I need to check world state and memory for established facts. **The world state says no records yet.**」
> 「**Let me search lorebook for 怀瑾 和 星辰之夜** to get details…」
> 「**Let me do a couple of searches to ground the scene.**」

时间线：`thinking(1384) → text(2836)`，`toolCall = false`，`stopReason = stop`。

**结论**：注入 → 形成意图这一环是**通的**（它读了每拍无条件注入的【世界状态】，发现为空，于是决定检索）。
断的是**意图 → 动作**。

---

## 三、断掉的四环（全部是拆路标时的误伤）

用户明述：「开头那个判定我一直都没打算拆，它的失效是我拆路标这个词的时候的误伤，把前提条件给弄没了。」

| # | 装置 | HEAD 位置 | 现状 |
|---|---|---|---|
| 1 | **`PLAN_CARD`** 末端注入 | `HEAD:engine.ts:266-273`，拼装于 `:716` | 已删 |
| 2 | **【梨园运行架构】** system 第 0 段 | `HEAD:assemble.ts:296-307` | 已删 |
| 3 | **`skill_read` 受理门** | `HEAD:engine.ts:1086-1092 / 1208-1230` | 已删，`forcedSkills` 成死变量（`engine.ts:574/757`） |
| 4 | **`ask判断` skill 全文** | `skills/ask判断/SKILL.md`（磁盘仍在） | **无路可达**（`每轮:true` 但非 resident，靠受理门送达） |

### 各环的原文（修复时按需取用，**只许删不许加**）

**1. `PLAN_CARD`**（`HEAD:engine.ts:266-273`）：

> 【第 1 步·规划】本拍还没有计划。读题、探索（工具自取）；用户这句输入引出的未定变量——取不同值这拍走向会分岔、且设定里查不到的——先 `ask` 请用户定，再用 `beat_plan` 列路标。你的任务是列出抽象的路标的同时为路标的具体内容留下充分的可发挥余地，让下面每个剧情轮次扮演路标时拥有极大的发挥空间以给用户带来更多的剧情可能性。没有戏的拍可 `draft_write` 一次交完；用户本轮在求方向/递笔的，直接 `ask`。

拼装（`HEAD:engine.ts:714-717`）：

```ts
const injWithCard = tools.length > 0 ? `${injection}\n\n${PLAN_CARD}` : injection;
const tailText = endsWithUser ? `${injWithCard}\n\n${history[history.length - 1].text}` : injWithCard;
```

**2. 【梨园运行架构】**（`HEAD:assemble.ts:296-307`），关键句：

> 首轮只规划：读题、探索、**请用户定夺**、列路标。

**4. `ask判断`** 三条件的第三条（`skills/ask判断/SKILL.md`）：

> **查不到**：卡、世界书、已演剧情里都没有答案；**查得到的先查**。

> ⚠ 「查得到的先查」这五个字是检索的真正触发句，现在锁在模型看不见的文件里。

---

## 四、问题 ②：执行意图被预设覆盖 —— 精确位置

`src/stage/engine.ts:660-665`（现状）：

```ts
const tailRuns = [
  { role: "user", text: tailText },        // = 动态注入 + 本拍用户原话
  ...phAll.filter(...).map(...)            // = 预设 after 段
];
```

**模型看到的最后一段话 = 预设 after 段（「开始写正文」）。** 梨园的数据块在它前面。

### ⚠ 必须先查清的关键事实（下个窗口第一件事）

**HEAD 里 `PLAN_CARD` 与预设 after 段的相对顺序是什么？**

- 第二步删掉了 `StageInjectionOptions.presetTail` 字段与 `【预设末端指令】` 注入块，并改为 after 段 role 保真。
- 所以 **HEAD 时期预设 after 段的位置与现在不同**，`PLAN_CARD` 当年可能确实排在更靠后的位置。
- **不查清这一点就动手 = 凭猜复原。**

取证方法：`git show HEAD:src/stage/engine.ts` 读 700–760 行的完整拼装，与磁盘 645–690 行逐行对照。

---

## 五、任务分解

### T1（先做）· 取证：HEAD 与磁盘的末端拼装顺序逐行对照

- **产出**：一张顺序表——HEAD 的末端消息序列 vs 现状的末端消息序列。
- **判据**：能明确回答「`PLAN_CARD` 在预设 after 段之前还是之后」。
- **不写代码。**

### T2 · 恢复变量判定的末端注入

- **改哪**：`src/stage/engine.ts`，恢复一个精简版 `PLAN_CARD`，拼进 `tailText`。
- **文案**（砍掉路标/draft 那一半，`beat_plan`/`draft_write` 均已不存在）：

  > 【第 1 步】读题、探索（工具自取）；用户这句输入引出的未定变量——取不同值这拍走向会分岔、且设定里查不到的——先 `ask` 请用户定。

- **铁律核查**：
  - 铁律一（禁堆叠提示词）：**送模文案净减**（原文 5 句 → 1 句），且是恢复误删，非新增。合律。
  - 铁律二（禁堆叠注入）：**这是把误删的既有注入时机放回去，不是新增时机**。用户已明述「一直没打算拆」。⚠ 但方向与第一步「拆循环」相反，**动手前须用户再确认一次**。
  - 铁律三：不涉及名单。合律。
- **验证**：新开会话 + 开场第一拍 + 思考 high。看 `rpTimeline` 是否出现 `thinking → tool → text`。
- **风险**：若 T1 查出预设 after 段在其后，则此注入仍会被覆盖，T2 无效 —— 那时问题升级为「梨园的指令要不要排在预设之后」，**那是第二步成果与第三步需求的正面冲突，必须交用户定案，不得自行决定**。

### T3 · 恢复 `ask判断` 的送达路径

- **问题**：`每轮: true` 但非 resident，受理门删后无路可达。
- **两个候选（不自行选，交用户）**：
  - (a) 改成 `resident: true` —— 全文进 system，每拍都在。**代价：system 变长，触铁律一判据**。
  - (b) 恢复某种强制读取 —— **但受理门锁在已删除的 `draft_append` 上，无处可挂**。
- **⚠ 现状遗留**：`forcedSkills` 仍在 `engine.ts:574/757` 计算但无消费者，属死变量。
- **附**：`skills/skill指导/SKILL.md` 已从磁盘删除，但 `{{可用skill}}` 的展开代码仍留在 `engine.ts:1300-1313`。

### T4（问题 ①）· 修补注入机制的缺口

按严重度排序：

| # | 缺口 | 证据 | 性质 |
|---|---|---|---|
| 4.1 | **【剧情记忆】是幽灵** | `assemble.ts:357` 告诉模型有这个块，**但无任何代码产出它**。`memoryRecallForTurn`（`src/memory/service.ts:324`）注释写「供 buildTurnInjection 注入」，而 `buildTurnInjection` **全仓不存在**。`src/memory/types.ts:54` 注释描述的行为同样无实现。 | **向量库的注入侧整条死了**，只剩 `memory_search` 工具那一半 |
| 4.2 | **登场名录实现走样** | `src/state.ts:276-288` 只列**已离场/已失去/已了结**的条目；在场的从不进名录；物品与剧情线 blurb 恒为空只出名字；新会话里整块不出现 | 用户原意是「记录所有出现的人/物/简要剧情，作为检索依据」，现实是一张**离场索引** |
| 4.3 | **codex 知识库无注入** | `loadStageMaterials` 的 entries 不含挂载 codex（`materials.ts:184`），只在 `lorebook_search` 后端临时并入（`engine.ts:1235-1243`） | 注入侧死 |

**4.1 的最小动作**：要么补上产出【剧情记忆】的代码，要么**删掉 `assemble.ts:357` 那句幽灵说明**（后者是纯删除、送模文案变短、合铁律一）。**先删幽灵，再谈补通道。**

---

## 六、注入现状全表（2026-08-21 普查）

| 通道 | 触发 | 块标题 | 活否 |
|---|---|---|---|
| 世界状态 | **每拍无条件** | 【世界状态】 | 活 |
| 登场名录 | 有「已离场」条目才出 | 【登场名录】 | 活but走样 |
| 相关设定 | **关键词匹配**（唯一一处） | 【相关设定】 | 活 |
| 设定集索引 | 准常驻（标题表，500 字预算） | 【设定集索引】 | 活 |
| 常驻世界书 | system 常驻；预设声明 worldInfo marker 时让位 | # 世界设定 | 活 |
| 前情提要 | 有 rp-summary 才出 | 【前情提要】 | 活 |
| 活跃面板 | 有面板才出 | 【活跃面板】 | 活 |
| 卡末端指令 | 卡里有才出 | 【卡作者末端指令】 | 活 |
| **向量库** | **无注入** | —（【剧情记忆】是幽灵） | **死** |
| **codex** | **无注入** | — | **死** |

**全仓只有 `scanEntries` 一处关键词匹配注入**，没有第二处。

---

## 七、供料现状的关键数字（别高估问题）

本机配置（一张卡 + 两本世界书，145 条）：

- **蓝灯（常驻）28 条 / 25632 字 —— 每拍无条件在场**
- 绿灯（关键词）90 条
- 关灯 27 条

⇒ 模型每拍手里有 2.5 万字世界设定，**不是「什么都不知道在编」**。真正缺的是长尾。
⚠ 「绿灯 12 拍里 10 拍零命中」这个数字**取样质量差**（一半是「看情况」「继续」这类短输入），
**是绿灯真坏还是取样坏，尚未查清** —— 这是问题 ① 下唯一值得先做的纯查证。

---

## 八、操作性约束（真做前置检索时必带）

实测小模型旁路调用延迟：中位 **1043ms**（最快 837ms），但 12 次中 **1 次卡 302 秒、1 次 HTTP 503**。
任何串在扮演之前的旁路调用**必须带超时 + 降级**，否则 provider 一抽风用户每拍干等。

---

## 九、本窗口已落地（未提交）

**第三步第二刀：末尾轮记账归场记。** 纯删除，恢复 `scribe-run.ts` 的原始设计（文件头：「台上零世界写入工具」）。

- `src/stage/tools.ts`：删 `world_state_update` 定义 → `writeTools` 只剩 `ask`
- `src/stage/workspace.ts`：删 handler + `patches` + `projectedState` + `baseState` + 两个 import
- `src/stage/engine.ts`：删「模型提交 patch 落账」整块；场记条件改无条件；摘两个 import
- **验证**：探针 `_probe-scribe.mts` 通过；**真模型双变体实测通过**（思考 high / off 各一，两个变体都正确落 `rp-state`）
- 另：`src/stage/tools.ts` 的 `ask` ② 锚点已由「规划阶段、列路标之前」改为「开场第一拍」（净减 6 字）——**实验证明无效**，是否保留待定

**测试**：40 红（基线 37，+3 全是「每拍 4 个 mock 响应」的队列错位，与第一步同类，非功能回归）。测试文件一字未改。

**工作区**：HEAD 仍 `2d065c4`，本窗口 + 上窗口改动全部未提交，**提交方案未拍板**。

---

## 十、未决 —— 必须交用户，不得自行决定

1. **T2 是否开工**（恢复注入点，方向与第一步相反）。
2. **若预设 after 段排在梨园注入之后**：梨园的指令要不要排到预设之后？这是第二步「预设主导输出形式」与第三步「梨园架构融入」的正面冲突。
3. **T3 用 (a) resident 还是 (b) 别的送达路径**。
4. `ask` ② 那个改动（「开场第一拍」）留还是回退。
5. 未提交工作的提交方案。
