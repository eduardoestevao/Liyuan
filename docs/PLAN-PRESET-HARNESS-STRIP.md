# 预设剥离 harness：编译后窄拆契约

> 2026-09-12 立项。前置分析见 `.liyuan-artifacts/preset-compiled/`（13 份预设的编译全文、逐块出处、统计）。
> 用户定案方向：**剥离预设的 harness 属性，只保留提示词属性**。本文是实施契约。

## 一、为什么这次能成（上次 8/14 死在哪）

- 8/14 死的是**判断载体**：对 242 个不透明 JSON 块做性质分类，208/242 落兜底；块名是作者词汇表（铁律三不许认）。
- 本次载体换成**编译后全文**：梨园装配器（`assemble()`）跑一遍，得到模型真正看到的 1~2 万字线性文档。8 个作者的预设编译后共享同一套「站点语法」，且作者用自声明标签（`<thinking_rules>`、`<main_output_format>`、`<writing_process>`…）把站点划清楚了。
- 溯源现成：装配结果逐 piece 带块 identifier，段落级声明可精确落回 `prompt_order[].enabled`（酒馆自己的开关，合法数据通道）。

## 二、编译后站点语法（13 份实测归纳）

```
[身份/破限] → [材料包装] → ⟦历史⟧ → [写作规则] → [思考协议] → [输出合约] → [assistant 预填]
```

| 站点 | 预设模拟的机制 | 梨园的真身 | 处置 |
|---|---|---|---|
| 身份/破限（含信件对） | 角色身份 | 梨园身份（分层共存） | **卡级 APPEND_SYSTEM.md** |
| 写作规则（文风/边界/nsfw/精神分析） | — 纯话 | — | **卡档案 AGENTS.md**（材料槽，弱约束） |
| 材料包装 + marker 槽位 | 上下文装配 | 梨园装配器 | **不动**（编译器本职） |
| 思考协议（思考模板/地板/` drastically 禁令`） | 规划-检查循环 | agent 循环 + 原生思考 | **停用** |
| 草稿协议（段前注释排练） | 迭代写作 | draft_append / draft_seal | **停用** |
| 输出合约（strict_format / w2g / fox_selc / StatusBlocks / safe 仪式） | 结构化输出 + UI 数据层 | MVU + 面板 + ask + 渲染管线 | **停用** |
| 记忆记账（summary / key_memory） | 状态记账 | 场记 + world_state + 摘要 | **停用** |
| 末条 assistant 预填 | 输出引导 | 已按 8/23 定案丢弃 | 现状即终态 |
| samplers | 采样配置 | config.samplers | 数据侧（转译已做） |
| 顶层 harness 字段（43~45 项） | 引擎行为 | 未消费 | 原包保真，如实报告 |

### 槽位语义（三分法，2026-09-12 用户定案）

- **SYSTEM.md = 引擎协议**（梨园自己发行：消息流约定、MCP 外设、工具纪律）
- **APPEND_SYSTEM.md = 身份契约**（预设身份 + 破限；强槽）
- **AGENTS.md = 这张卡的全部**——世界 + 写法（写作规则弱约束入此，与卡作者末端指令同一家）

## 三、流程（窄拆五步）

1. **编译**：`assemble()` 跑一遍预设 + 当前卡材料，得全文 + 逐块溯源。
2. **声明**：模型读编译全文，逐段声明站点类别（上表六类 + 破限 + 包装）；**拿不准一律留，破限一律不碰**。声明落数据（每段 → 块 identifier → 处置）。
3. **过目**：用户看声明清单（每份预设十来个段落的清单，不是 250 块），可改可否。
4. **落盘**：转译分流——身份→`cards/<卡>/APPEND_SYSTEM.md`，写作规则→卡 AGENTS.md 新板块，停用清单→`prompt_order[].enabled` 或转译报告。原文留 `assets/presets/` 永不改动。
5. **验证**：甲乙双配置实验（同卡同输入同模型）：甲＝剥离后、乙＝整份。量四样：思考量 / 工具使用 / 嗓子 / 正文落法。

## 四、铁律自检

- 铁律一：送模文案只减不增（停用即减）；不加任何催告。
- 铁律二：不新增注入点——身份/规则落既有文件槽，停用走酒馆 `enabled` 开关，声明走既有转译通道。
- 铁律三：判断权在模型（声明一次）+ 用户（过目），落成可见数据；不建块名名单。
- 铕律四：全集＝任意编译后文档的段落；负责人逐站明确（每站有梨园原生主人）；未见过的卡＝声明拿不准就留，下行有界（最差＝今天）。

## 五、已知代价（认下，不当 bug 查）

1. 每份预设过用户一眼（这次看的是段落清单，不是 250 块）。
2. 停输出合约后，靠预设教模型吐状态栏的卡会停止吐——梨园里这是预期行为（MVU/面板接管）。
3. 破限信件对转译后被拍平成文件内引文（role 弱化）；保真 role 需新注入通道，违律二，不做。
4. 转译按卡落（宏必须在卡上求值）；换卡、预设更新都触发重转，触碰用户手改过的 AGENTS.md 时列为冲突不静默合并。
5. AGENTS.md 一落盘卡即进文件模式（刀3 规则），面板改字段不再反映到档案。

## 六、状态

- 2026-09-12：立项，13 份预设编译分析完成，站点语法与去处表定稿（本文件）。
- 2026-09-12：首个手工样本走到**声明完成、待过目**——双人成行 v10.0（assets 副本），
  声明清单在 `.liyuan-artifacts/preset-compiled/双人成行-声明清单.md`。
- 2026-09-12：**过目通过，落盘完成**（用户点名卡：大乾风华录 Ver1.7，user＝怀瑾）。
  - `cards/大乾风华录 Ver1.7/APPEND_SYSTEM.md` 新建：身份/破限 2 块 1,623 字（main 信＋nsfw 回信，宏按卡求值）。
  - `cards/大乾风华录 Ver1.7/AGENTS.md` 追加「预设写作规则」板块：12 块 10,137 字（原 21,213 字 → 31,498 字）；追加前备份到 `.liyuan-artifacts/preset-compiled/落盘备份/`。
  - `cards/大乾风华录 Ver1.7/.liyuan/转译报告-双人成行v10.0—青云上-(1).md`：逐块去向＋停用 10 块 3,423 字及理由＋已知残留＋甲乙切换法。
  - 该卡 config 本就无 preset 指针（无预设态）⇒ 落盘后即**甲（剥离态）**；乙＝config.preset 指回预设＋移开两处转译段。
  - 预设原文零改动。采样参数未迁 config（实验不引入采样差异）。
- 2026-09-12：**甲乙实验完成（拍 1 结论性，拍 2 乙因 DeepSeek 402 余额耗尽缺失）**。
  同卡同输入同模型（官方 deepseek-v4-pro）、隔离双工作区（`.liyuan-artifacts/preset-ab/`），对比报告在该目录 `对比报告.md`。四项判据：
  - 思考量：甲拍 1 **6,216 字** vs 乙 **69,697 字（11.2 倍）**；甲两拍合计 25,858 字全收拍，乙一拍未收完（402 断供，phase=error）。
  - 乙的思考主体是**对抗**：`injection`×25、`jailbreak`×9——模型把 post-history 的预设段判为恶意注入、裁决后忽略之、跟随卡文风。同一内容在甲的材料位被平静引用、零对抗（**位置改变行为，验证槽位设计**）。
  - 11 问 COT 零字面执行（`【问题】`×0）但仪式税照收——地板靠「反复确认合规」生效，不靠模板。
  - 文风两边都在卡侧；状态栏（卡所有）两边照常；乙探索工具反而更多（memory_search＋lorebook_search）。
  - **判据四项全拉开：拆的路活，函数法留备胎。**
- 2026-09-12：**声明步骤产品化完成**（窄拆全链路进产品，提交 `4d02c01`；⚠ 次日用户定案 UI 改回直用，见下条——端点与纯函数保留）：
  - REST：`POST /api/presets/declare`（调 `restHost.runSideText` 旁路模型声明，草稿落本卡 `.liyuan/预设声明-*.json`，失败零落盘）；`GET /api/presets/declaration`（回读）；`POST /api/presets/translate` 加 `declaration` 分支（先查两处冲突再动笔；无档案的卡用投影打底＋板块，刀3 文件模式语义不破；overwrite 时板块替换不叠加）。不带 declaration 的旧整份转译原样保留。
  - 前端：预设库每份预设加「声明」按钮 → 内联过目清单（站别下拉可改、汇总计数）→「按声明转译」（内联卡片非弹窗，守前端观感规则）；旧「转译」按钮确认语改「整份收入，不剥离机制」。
  - 验证：`test/preset-declare.test.ts` 5/5；全量 794 项 790 过 4 红——4 红全部为实卡环境性既有失败（stash 排除法坐实与本改动无关：本机卡池下 stash 前后同样红）；前端 typecheck＋生产构建过；faux 模型 e2e（隔离端口＋假 openai-completions）：声明 29 段落草稿、GET 回读、分流落盘三产物、冲突保护（APPEND 已存在→拒；板块已存在→拒；overwrite→板块原位替换不叠加）全部通过，证据 `.liyuan-artifacts/preset-declare-e2e/`。
  - 真模型声明（DeepSeek 余额耗尽未跑）：充值后对任意预设点「声明」即验。
- 2026-09-12（深夜·用户定案修正）：**预设库 UI 改回直用**——「声明/转译」按钮退场，改为
  **装载**（设为活动预设，config.preset）＋**保存**（改动写回预设文件）＋**卸载**（停用，文件保留）；
  编辑器条目带来源标注（预设块 / 梨园材料槽），不标注就没法判断哪条能动。窄拆的声明/分流
  REST 端点与纯函数保留在后台（机制已验证，甲乙数据在案），不再从界面进入。
  **剥离开销的正确入口回到预设自身的块开关**——与窄拆法原定的数据通道（prompt_order[].enabled）
  一致；用户在编辑器里手动停用机制段即得甲态。REST 冒烟七步验证装载→编辑→保存→卸载全链路。
- 2026-09-12（深夜）：cpSync 崩溃修复——`src/fs-copy.ts` copyPathSafe 替换 backup 恢复两处与
  卡迁移跨盘搬移一处；中文目录回归测试入库（`test/fs-copy.test.ts`）。提交 `ea42259`。
- 顺带发现（与预设无关）：node 24.14.1 `fs.cpSync` 遇非 ASCII **目录**路径原生崩溃 0xC0000409（中文文件名无恙）——已修（上条）。

