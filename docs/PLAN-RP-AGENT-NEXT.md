# RP agent 当前阶段：实际体验与反馈调整

2026-09-08 用户重新定序；同日「回到 agent 架构」已全部落地。两种写卡工具调查与 [资源编辑第一切片](CARD-AUTHORING-SLICE1.md) 已完成。2026-09-09 按用户定案，**主会话写卡模式已实现并验证**，见 [共享记录与模式可见性](CARD-AUTHORING-MODE.md)。用户随后决定：**「接下来就是根据实际体验来各种调整了」**。本文是下一窗口的执行入口。

## 一、当前阶段（2026-09-09 用户定案）

以用户实际使用中的问题作为下一项工作的入口，覆盖扮演、写卡、临场维护和页面交互。先复现完整操作链，再用实际请求、工具结果、界面和持久化记录定位问题归属，做针对性修改并复验。下表保留原路线与完成状态；剩余能力由实际需求确定优先级，不自动开始下一批功能或架构扩展。

| 顺序 | 工作 | 当前状态 |
|---|---|---|
| 1 | 回到 agent 架构（原「无预设扮演体验」的实际形态） | **已完成**，八提交：刀1 `9985258` SYSTEM.md 立位＋默认预设退役；刀2 `72679dd` 用户提示词两级文件＋预设转译；刀3 `f776845` 卡档案 AGENTS.md＋助手生成路径；刀4 `d2619dc` 协议判定数据化；`3055ec7` 提示词面板定名分栏；`2e52067` SYSTEM.md 越权修正（环境底座最小化，扮演定义入默认 APPEND）；`1b12982` 提示词条目引擎＋双视图编辑器；`77b496b` 面板去噪＋内联新增。主档 docs/PLAN-AGENT-SLOTS.md |
| 2 | **角色卡制作与代码能力** | 两种工具调查、资源编辑第一切片、主会话写卡模式已实现并验证，尚未提交。两模式共用记录，写卡开放工具过程，续演封闭维护内容；当前转入实际体验调整，完整创作与关联资源的缺口按需求处理 |
| 3 | 前端优化 | 按实际使用反馈确定范围；提示词板块的观感规则已立（见记忆） |
| 后置 | 预设处理 | 已降为「预设库＝转译素材」；遗留编辑器只在 config.preset 仍指向时出现 |
| 延期 | 小说创作模式 | 继续延期 |

## 一A、第二步的代码维度（用户点名，调查中心）

很多角色卡背后有代码。以下是本轮调查的原始入口，结论及实施状态见 [角色卡制作计划](PLAN-CARD-AUTHORING.md)：

- `tavern_helper.scripts[]` 页面级脚本宿主（ScriptHost，0×0 同源帧）——悬浮球/交互组件
- 卡内嵌脚本（`extractAuthorScripts`）——MVU prefault 播种、regex 等
- MVU 变量体系（`src/mvu.ts`：变量树、更新规则、schema prefault）——与场记的归属分工
- 状态栏 HTML/CSS/JS（`cardfront.ts` 快照、作者正则、显示管线）
- 作者正则 `regex_scripts`（送模侧 promptRules／显示侧两套应用点）
- 卡档案生成路径（刀3 的「让助手生成」）目前只搬文字——代码资产如何进档案/如何被 agent 理解与修改，是调查要回答的第一个问题

调查已按「代码资产清单＋执行环境＋agent 读写/验证通道」完成。采用共享资源服务与既有消息帧/脚本宿主，没有移植第三方的强制创作流程或另起模型循环。当前主要入口是主 agent 自主/手动切换的写卡模式，提供 `card_project` 与原生文件工具；卡详情内联「代码与资源」。独立助手不再承担主要写卡入口。

## 二、第一步：无预设扮演体验

先根据用户指出的问题，复现 pi 回归后梨园实际如何扮演，再做有依据的微调。当前没有收到完整的症状清单，不能把下面的观察面写成已确认缺陷或预定修复方案。

- 观察自然输入下的剧情推进、回应与节奏，工具使用是否服务于正在发生的剧情。
- 观察分段、提问、用户回答、改道与续写的完整体验；保留用户选择及其所属拍次。
- 核对停止、重生成、上一拍修改和继续扮演时，正文、实际送模历史与用户看到的内容是否一致。
- 对每个确认的问题记录复现条件、实际请求/工具/稿件证据、机制归属和改前改后结果；不凭调用次数或一次成功样本宣布体验已经完成。

原计划“只看结构指标”的口径用于之前的底座验收。**本阶段要解决用户实际扮演体验，不能以工具通路已通过来代替体验判断**；同时不引入文风评分器、逐拍审稿模型或强制工具仪式。

对照 `PLAN-ROUND-FLOW.md`，继续服务于“看见上文 → 作出选择 → 根据实际变化继续演”，计划是假设。pi `AgentSession` 继续负责模型与工具循环，场记负责定稿后的世界状态记账，世界线导航仍归用户。

## 三、第二步：角色卡制作与代码产品能力

路线见 [角色卡制作与修改](PLAN-CARD-AUTHORING.md)，实际实现与边界见 [资源第一切片](CARD-AUTHORING-SLICE1.md) 和 [主会话模式](CARD-AUTHORING-MODE.md)。这一阶段包括完整卡创作，以及围绕卡开发能实际运行的代码和交互作品；资源与模式入口完成不代表本阶段全部完成。

已完成已有卡的原文资源展开、版本保护、源码编辑、测试预览、应用/撤回，以及世界书导出保真与等长脚本重载。浏览器已经验证状态栏搜索、页面组件、PNG 导出后真实导入及再次交互；预览的命令和变量通道与正式剧情隔离。

两种模式共用主会话底层记录。写卡时可见原始工具过程，回到扮演后整段维护请求、讨论、代码与回执重新封闭；场记、摘要、剧情记忆与剧情导航共用这一归属规则。原生循环内切换、卡片应用后重载、停止/重开/排队均已验证。官方 DeepSeek 已自主完成基础卡字段修改及隔离续演。

尚未覆盖从零创作所需的按需资料、新增资源/元数据、源码构建、依赖携带，以及 MVU schema/初值/规则/各开场/界面的关联验证；原卡外部修改后的工程同步也未实现。写卡长历史预算及按需展开同样未实现。这些是能力边界，是否补齐及先后顺序由真实任务决定。EJS、逐消息变量和更广 TavernHelper 语义按实际作品推进，不承诺任意酒馆卡已经兼容。

## 四、第三步：前端优化

面向梨园应用本身的交互、布局、工具过程呈现、稿件与选择操作、刷新和恢复体验进行调整。具体范围依据前两阶段的使用反馈与用户后续指示确定。本阶段不预设重做整套 UI 或更换前端技术栈。

## 五、已经保存的开发基线

较早的 pi 回归基线提交为 **`1a3142d`**（`feat: 回归 pi 0.84.4 并补齐 RP 稿件与上一拍修订`），包含前三刀和上一拍修订。该开发提交未更改发布版本号，未推送或发布；本轮写卡开发的基线与验证见本节末。

- 卡项目化与跨会话记忆此前已提交；主循环已回到原生 `AgentSession`，七个 vendored runtime 包升级至 **0.84.4**。
- 本拍持久稿件、计划、探索、原文取证、卡级技能、记忆检索与纠错、用户恢复及上下文可观测/剪枝已有实现。
- `previous_draft_read` / `previous_draft_edit` 只定位当前分支最近一拍已定稿回复；修改落回原回复，显示与后续上下文同步。ask 实际回答的跨拍归属已修复。
- **上一拍修订仍是正文操作**：不会自动重算账本、MVU、面板数据或已存记忆。事实修改后的关联数据协调尚未实现，不在本次交接中擅自确定方案。

该历史基线的验证见 [上一拍修订记录](PI-RETURN-PREVIOUS-DRAFT.md)：729 项测试，727 通过，仍有两项既有实卡失败；前端类型检查与生产构建通过；后端广域 TypeScript 的 33 条既有诊断无新增。官方 DeepSeek 三次上一拍回改复验通过，保留未修改的后半段，续演与重连读取同一正文。以上保留为历史结果，本次文档与记忆归档没有重复运行测试或实弹。

两项既有测试失败：`实卡: first_mes/备选开场白 一律 html 皮肤、零 status 段`；`实卡：初值只写在 schema prefault 里的卡能被播种`。

实现与证据索引：[第一刀](PI-RETURN-KNIFE1.md)、[第二刀](PI-RETURN-KNIFE2.md)、[第三刀](PI-RETURN-KNIFE3.md)、[无预设自主验收](PI-RETURN-AUTONOMY-DEEPSEEK.md)、[上一拍修订](PI-RETURN-PREVIOUS-DRAFT.md)。实际模型正文、请求和用户卡仅在本机忽略目录留存。

写卡开发基于后续提交 **`5c45fc1`**，改动尚未提交。最新验证是 **773 项测试、771 通过、两项既有失败**；针对性测试 148 项通过，前端类型检查和构建通过，后端同参数诊断 38 → 35、无新增。真实页面完成自动切换、资源应用及实时界面更新；官方 DeepSeek `deepseek-v4-pro` 完成自然切入、基础字段修改、切回和续演，共 9 次请求（含场记）。复杂代码创作质量仍需真实任务继续验收，证据见模式记录。

## 六、下一窗口接手

1. 先读根 `AGENTS.md` / `CLAUDE.md` 与本文，再读 [PLAN-AGENT-SLOTS.md](PLAN-AGENT-SLOTS.md)、[PLAN-CARD-AUTHORING.md](PLAN-CARD-AUTHORING.md)、[CARD-AUTHORING-SLICE1.md](CARD-AUTHORING-SLICE1.md) 和 [CARD-AUTHORING-MODE.md](CARD-AUTHORING-MODE.md)。
2. **根据实际体验与用户反馈调整**。调查、资源编辑第一切片与主会话模式已完成，不重复从零调查。先接住用户的具体问题、复现并定位，再决定修改范围；关联资源、源码构建、长历史预算等保留为能力缺口，不自动扩建。沿用同一记录的模式可见性，复验从维护到续演的完整链路。
3. 提示词板块已收口；前端观感规则见记忆（说明长句/路径不上界面；弹窗类交互禁用，用内联卡片）。
4. 实弹继续用户的**官方 DeepSeek 渠道**（含主会话写卡）；不使用 Claude 额度测试。读思考记录先按 `READING-THINKING.md`。
5. 保留 `CLAUDE.md` 四条铁律；不恢复 `draft_check`、LLM 完成裁判、强制每拍计划/技能、提示词催告或作者措辞名单。改动前明确问题全集、负责人及未见过的卡上的行为。
6. 用户常用服务（7620）本窗未重启；资源编辑与写卡模式需要重启后端并刷新页面才能完整生效。隔离测试服务均已关闭，本轮未发布或部署。
